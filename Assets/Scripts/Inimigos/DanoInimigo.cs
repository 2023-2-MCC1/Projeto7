using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DanoInimigo : MonoBehaviour
{/*
    public Slider barraDeVida;
    public VidaJogador vidaJogador;
    public static int vidaInimigo = 30;
    public int dano = 2;
    public AudioSource somDanoPescador;
    public AudioSource somDanoInimigo;

    public void tomarDano(int dano)
    {
        vidaInimigo -= dano;
        somDanoInimigo.Play();
    }
    private void OnCollisionEnter2D(Collision2D colisao)
    {
        if(colisao.gameObject.tag == "Jogador")
        {
            vidaJogador.TomarDano(dano);
            somDanoPescador.Play();
        }
    }

    void Update()
    {
        barraDeVida.value = vidaInimigo;

        if (vidaInimigo <= 0)
        {
            Morte();
        }

    }

    void Morte()
    {

        barraDeVida.gameObject.SetActive(false);
        Destroy(gameObject);

    }*/
}
